version: '3.8'

services:
  cyber-rag-studio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cyber-rag-studio-prod
    ports:
      - "8080:80"
    restart: always
    environment:
      - NODE_ENV=production
